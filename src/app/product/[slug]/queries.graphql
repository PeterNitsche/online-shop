query GetProductSlugs {
  getLandingProducts {
    products {
      slug
    }
  }
}

query GetProduct($slug: String!) {
  getProductBySlug(slug: $slug) {
    id
    title
    price
    currency
    brand
    inStock
    images {
      public_id
      secure_url
    }
    description
  }
}
