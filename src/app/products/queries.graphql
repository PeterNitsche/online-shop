query GetProducts {
  getLandingProducts {
    products {
      id
      slug
      title
      price
      currency
      averageRating
      images {
        public_id
        secure_url
      }
    }
  }
}
